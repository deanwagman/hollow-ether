DATABASE_URL="postgresql://ethernetic:ethernetic@localhost:5432/ethernetic?schema=public"

# LLM (Bedrock or mock) — used by NarrativeService on /interact
LLM_PROVIDER=mock
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=us.amazon.nova-lite-v1:0
LLM_MAX_TOKENS=512

# Game narrative on /interact (llm = Bedrock Luminia; mock = keyword dialogue)
NARRATIVE_PROVIDER=mock
# Optional: AWS_PROFILE=your-profile
